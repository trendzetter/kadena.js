{
  "changes": [
    {
      "packageName": "@kadena/client",
      "comment": "Remove `cmd` from the transaction when the properties of this transaction change, so it gets recalculated. Reset possible signatures when a finalized transaction gets changed, because the signatures do no longer match the transaction",
      "type": "patch"
    }
  ],
  "packageName": "@kadena/client"
}
